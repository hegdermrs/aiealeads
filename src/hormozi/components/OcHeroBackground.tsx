/** Overcode hero gradient stack — uses their published SVG assets */
export default function OcHeroBackground() {
  return (
    <div className="oc-hero-bg" aria-hidden="true">
      <div className="oc-hero-bg-sides" />
      <div className="oc-hero-bg-top" />
    </div>
  );
}
